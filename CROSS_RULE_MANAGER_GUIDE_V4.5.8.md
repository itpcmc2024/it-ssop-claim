# Cross Rule & Knowledge — คู่มือใช้งาน V4.5.8

## 1. Auto Fix ตาม Logic
ใช้กับ Error ที่ระบบคำนวณค่าที่ควรแก้ได้จากข้อมูลภายใน ZIP เช่น C08, S32, S33, R04, T33, T44, T45

ก่อนแก้ ระบบจะแสดง:
- Logic ของ Error
- ฟิลด์ต้นทาง → ฟิลด์ปลายทาง
- ขั้นตอนที่ระบบจะทำ
- สิ่งที่ระบบจะไม่เปลี่ยน
- ข้อควรระวัง

ผู้ใช้ต้องกดยืนยันก่อน ระบบตรวจ Cross ใหม่หลังแก้ และยัง Undo ได้ก่อนบันทึก MD5/ZIP

## 2. Guided Replace
ใช้เมื่อผู้ใช้ทราบค่าที่ถูกต้องเอง เช่น C07: Hmain เดิม HA0000009 ต้องเปลี่ยนเป็น 60002

เลือก Error Code → เลือกค่าเดิมที่ซ้ำ → กรอกค่าใหม่ → แทนค่าที่ตรงกันทั้งหมด
ระบบแก้เฉพาะจุดที่ถูกตรวจเป็น Error Code นั้น ไม่ไล่ Replace ทั้ง ZIP แบบไม่มีขอบเขต

## 3. กฎตรวจ Cross & Knowledge
เหมาะกับ Error ใหม่ที่ตรวจจากฟิลด์เดียว เช่น ค่าว่าง, เท่ากับ, ไม่เท่ากับ, Regex

ขั้นตอน:
1. กรอก Error Code
2. กด ดึง Knowledge
3. เลือก Section / Field / เงื่อนไข / ค่าที่ตรวจ
4. กด เพิ่มกฎตรวจ

ถ้า Knowledge ยังไม่มี:
- เปิด/แก้ Knowledge เพื่อบันทึกเอง หรือ
- ส่ง Error ใหม่เพื่อค้นคว้า แล้ว Import Knowledge Pack กลับมา

กฎข้ามหลายแฟ้ม/กฎคำนวณยอดยังต้องเป็น Built-in Rule เพื่อป้องกัน Logic ผิด

## 4. Workflow กลางของ SSOP Editor
ทุก Rule Profile ใช้ลำดับเดียวกัน:
แก้/เติมข้อมูล → ตรวจตามโมดูล → บันทึกไฟล์ + MD5 → บันทึก ZIP ลงเครื่อง
